/** @type {import("prettier").Config} */
const config = {
    semi: true,
    singleQuote: false,
    tabWidth: 4,
    useTabs: false,
    trailingComma: "all",
    printWidth: 120,
    bracketSpacing: true,
    arrowParens: "always",
    endOfLine: "lf",
    plugins: [],
};

export default config;
