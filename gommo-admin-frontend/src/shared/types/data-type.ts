/**
 * Tipos de dado usados em grids, forms e formatação/máscara de valores.
 */
export enum DataType {
    STRING = "STRING",
    INTEGER = "INTEGER",
    DECIMAL = "DECIMAL",
    CURRENCY = "CURRENCY",
    PERCENT = "PERCENT",
    BOOLEAN = "BOOLEAN",
    SELECT = "SELECT",
    CNPJ = "CNPJ",
    CPF = "CPF",
    CEP = "CEP",
    PHONE = "PHONE",
    EMAIL = "EMAIL",
    DATE = "DATE",
    DATETIME = "DATETIME",
    UUID = "UUID",
    /** Badge visual / status textual. */
    BADGE = "BADGE",
}
